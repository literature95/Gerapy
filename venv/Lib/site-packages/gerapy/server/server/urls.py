"""server URL Configuration

The `urlpatterns` list routes URLs to views. For more information please see:
    https://docs.djangoproject.com/en/1.11/topics/http/urls/
Examples:
Function views
    1. Add an import:  from my_app import views
    2. Add a URL to urlpatterns:  url(r'^$', views.home, name='home')
Class-based views
    1. Add an import:  from other_app.views import Home
    2. Add a URL to urlpatterns:  url(r'^$', Home.as_view(), name='home')
Including another URLconf
    1. Import the include() function: from django.conf.urls import url, include
    2. Add a URL to urlpatterns:  url(r'^blog/', include('blog.urls'))
"""
import sys  # 导入 sys 模块，用于访问命令行参数
from django.conf.urls import url, include  # 导入 url 和 include 函数，用于定义 URL 路由
from django.contrib import admin  # 导入 Django 管理后台模块

urlpatterns = [  # 定义 URL 路由模式的列表
    url(r'^admin/', admin.site.urls),  # 将以 /admin/ 开头的请求路由到 Django 管理后台
    url(r'^', include('gerapy.server.core.urls')),  # 将根 URL 路由到 gerapy.server.core 应用的 URLconf
]

if 'runserver' in sys.argv:  # 检查是否通过 runserver 命令启动服务器
    # start scheduler
    from gerapy.server.core.scheduler import sm  # 导入调度器模块
    sm.start()  # 启动调度器
